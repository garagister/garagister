<%= simple_form_for (@search) do |f| %>
  <%= f.input :intervention, collection: interventions.name %>
  <%= f.input :ville, collection: villes.name %>
  <%= f.submit "Obtenir trois devis", class: "btn btn-yellow" %>
<% end %>
